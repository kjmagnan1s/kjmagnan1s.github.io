# The /c/* rewrite to /c/index.html lives in the Worker now, not here.
# Cloudflare's assets layer rejects it as an infinite loop: the rewrite target
# gets its `/index` stripped, which re-enters the same rule. src/worker/index.ts
# serves /c/index.html for those paths instead, via run_worker_first in
# wrangler.jsonc. Do not add that rule back or `wrangler deploy` fails.

# Old-design pages retired on 2026-09-25. The redesigned home page and /resume
# replaced them; their source files stay in the repo with `published: false`.
/about/                      /#about  301
/about                       /#about  301
/work/*                      /resume  301
/work                        /resume  301
/building/                   /#do     301
/building                    /#do     301
/play/                       /#try    301
/play                        /#try    301
/contact/                    /#about  301
/contact                     /#about  301
